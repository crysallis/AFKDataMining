# Python
__pycache__/
*.pyc
*.pyo
venv/

# SQLite WAL / shared memory (live DB files, not for version control)
*.db-shm
*.db-wal

# Debug screenshots generated at runtime
src/screen_debug.png
src/debug_*.png
src/topright_debug.png
src/test_capture.png
screen_tmp.png

# Debug artifacts anywhere (root-level images + scratch experiment scripts)
debug_*.png
debug_*.py

# Live log output
scraper.log

# DB file itself (shared with bot, not committed)
guild.db

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json
